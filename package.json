{
  "private": "true",
  "scripts": {
    "build": "netlify build",
    "postinstall": "cd website; echo 'installing website dependencies'; npm i; echo 'installing plugin dependencies'; cd plugins/pretalx-fetch; npm i"
  },
  "devDependencies": {
    "netlify-cli": "^2.58.0"
  }
}
