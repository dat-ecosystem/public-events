{
  "private": "true",
  "scripts": {
    "build": "netlify build",
    "postinstall": "cd website; echo 'installing website dependencies'; npm i"
  },
  "devDependencies": {
    "netlify-cli": "^2.58.0"
  }
}
