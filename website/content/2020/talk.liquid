---
pagination:
    data: 2020.talks
    size: 1
    alias: talk
permalink: "2020/talk/{{ talk.code }}/"
layout: layout-2020
---

<h1>{{ talk.title }}</h1>

<h2>Abstract</h2>
<div class="abstract">
  {{ talk.abstract | md }}
</div>

<h2>Description</h2>
<div class="description">
  {{ talk.description | md  }}
</div>

<div class="duration">
  Expected duration: {{ talk.duration }} hours
</div>

<div class="slot">
  Time slot:
  <span class="when-tz" data-start="{{talk.slot.start}}" data-end="{{talk.slot.end}}">
    {{ talk.slot.start }} - {{ talk.slot.end }}
  </span>
</div>

<h4>Speakers:</h4>
<ul>
  {% for speaker in talk.speakers %}
  <li>
    <a href="/2020/speakers/#{{speaker.code}}">{{speaker.name}}</a>
  </li>
  {% endfor %}
</ul>

<h4>Resources:</h4>
<ul>
{% for resource in talk.resources %}
  <li>
    <a href="{{resource.resource}}">{{resource.description}}</a>
  </li>
{% endfor %}
</ul>

